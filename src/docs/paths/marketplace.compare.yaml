/marketplace/compare/{creatorName}:
  get:
    tags:
      - Marketplace
    summary: Compare items of a creator across titles
    description: Returns a mapping from title alias to that creator’s items.
    security: [ ]
    parameters:
      - in: path
        name: creatorName
        required: true
        schema:
          type: string
    responses:
      "200":
        description: OK
        headers:
          ETag:
            schema:
              type: string
        content:
          application/json:
            schema:
              type: object
              additionalProperties:
                type: array
                items:
                  $ref: "#/components/schemas/Item"
      "404":
        description: Creator not found
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Error"
      "500":
        description: Server error
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Error"
