FeaturedServer:
  type: object
  properties:
    name:
      type: string
    id:
      type: string
    items:
      type: array
      items:
        $ref: "#/components/schemas/Item"
  required:
    - name
    - id
    - items
